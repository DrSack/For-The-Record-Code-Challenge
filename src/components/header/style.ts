'use client'

import { styled, Toolbar } from "@mui/material";

export const StyledToolbar = styled(Toolbar)(() => ({
  backgroundColor: 'white',
  color: 'black'
}));

export const StyledTypography = styled(Toolbar)(() => ({
  fontSize: 20
}));
